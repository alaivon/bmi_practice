<% if current_user.grade.nil? %>
	
<div id="form">
	<h1><%= @school.name %></h1>
	<h3>New Class</h3> 
	<hr>

	<%= render 'form' %>
	<hr>

	<%= link_to "Back", @school, class: "back"  %>
</div>

<% else %>

<h2 class="text-center">You have already created a class, you cannot create again! </h2>

<% end %>

